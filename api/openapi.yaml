openapi: 3.0.3
info:
  title: Corporate Event Ticketing System API
  description: 企業活動票務系統後端 API 規格，前端開發可依此 Mock，後端依此實作。
  version: 1.0.0
servers:
  - url: http://localhost:8001/v1
    description: 本地開發環境
paths:
  /auth/login:
    post:
      summary: 使用者登入
      tags:
        - Auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                employee_id:
                  type: string
                  example: "EMP001"
                password:
                  type: string
                  example: "password123"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"

  /events:
    get:
      summary: 取得活動列表
      tags:
        - Events
      parameters:
        - in: query
          name: status
          schema:
            type: string
            enum: [draft, published, closed, ended]
      responses:
        "200":
          description: OK

    post:
      summary: 建立新活動 (Manager)
      tags:
        - Events
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEventRequest"
      responses:
        "201":
          description: Created

  /applications:
    post:
      summary: 申請票券 (Employee)
      tags:
        - Applications
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_id:
                  type: string
                  format: uuid
                ticket_type_id:
                  type: string
                  format: uuid
                quantity:
                  type: integer
                  minimum: 1
                idempotency_key:
                  type: string
      responses:
        "200":
          description: 已存在的申請或同步模式成功
        "201":
          description: 同步模式成功建立申請與票券
        "202":
          description: Queue mode：已進入 Redis Stream waiting room，背景 worker 將依序處理
        "403":
          description: 不符資格
        "409":
          description: 票已售罄


  /applications/queue/{idempotency_key}:
    get:
      summary: 查詢排隊申請狀態 (Employee)
      tags:
        - Applications
      security:
        - BearerAuth: []
      parameters:
        - in: path
          name: idempotency_key
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Queue status found
        "404":
          description: Queue status not found

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    CreateEventRequest:
      type: object
      required:
        - title
        - venue
        - publish_time
        - start_time
        - apply_deadline
        - end_time
        - ticket_types
      properties:
        title:
          type: string
        description:
          type: string
        venue:
          type: string
        publish_time:
          type: string
          format: date-time
          description: Must satisfy publish_time <= start_time < apply_deadline <= end_time.
        start_time:
          type: string
          format: date-time
        apply_deadline:
          type: string
          format: date-time
        end_time:
          type: string
          format: date-time
        region_restriction:
          type: string
          nullable: true
        max_tickets_per_person:
          type: integer
          minimum: 1
        ticket_types:
          type: array
          minItems: 1
          items:
            type: object
            required:
              - name
              - total_quota
            properties:
              name:
                type: string
              total_quota:
                type: integer
                minimum: 1
    AuthResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: object
          properties:
            access_token:
              type: string
            user:
              type: object
              properties:
                id:
                  type: string
                employee_id:
                  type: string
                role:
                  type: string
                  enum: [employee, event_manager, hr]