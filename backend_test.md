- 後端中除了 *_test.go 以外，不許更動其他檔案
- 測試後端時需要在wsl環境下進行測試，windows環境沒有安裝GO
- 寫測試時不需要考慮目前的業務邏輯是否已經實現，直接按照我的要求寫即可，沒實現是其他人的事情
- 寫測試時請參考目前已有的測試進行切分，不要所有function都寫在同一個檔案裡面
- 測試檔案中的所有註解、說明等使用中文進行撰寫
- 寫完後說明新測試的流程
- 子測試應該要在測試時輸出對應的資訊，詳細可以參考event_test.go
- 寫完測試後執行相關測試，這時候可以跳出沙盒外面執行
- 執行完畢後簡單講一下為什麼目前實作會通過/不通過新寫的測試
- QR Code 核銷目前後端仍保留裸 UUID token 相容性；若之後補動態 QR 測試，請同時覆蓋合法 `qr_token|otp`、錯誤或過期 OTP 回傳 `EXPIRED_QR`，以及既有 UUID token 相容流程
